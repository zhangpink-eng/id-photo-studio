# 环境变量配置示例
# 复制为 .env.local 或 .env.production 使用

# === 云端抠图 API（推荐） ===
# 设置后，浏览器不下载 168MB 模型，照片上传到服务器处理
# 服务器部署参考: server/ 目录 + deploy-server.sh
# NEXT_PUBLIC_REMOVE_BG_API=https://your-server.com:3001

# === AI 模型 CDN（可选，浏览器模式时使用） ===
# 留空则从代码 /public/models/ 加载
# NEXT_PUBLIC_MODEL_CDN_URL=https://cdn.yourdomain.com/
